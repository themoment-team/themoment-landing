# --- Where the site lives ---------------------------------------------------
# REQUIRED once there is a custom domain, and not optional the way the rest of
# this file is. On Vercel the production host is read from
# VERCEL_PROJECT_PRODUCTION_URL, which keeps naming the *.vercel.app host even
# after a domain is attached — so with this unset the canonical link, the
# sitemap entry, the Open Graph image and the structured data all name an
# address nobody visits, and the two hosts compete for the same search result.
# No trailing slash.
NEXT_PUBLIC_SITE_URL=

# --- Search engines ---------------------------------------------------------
# Verification tokens, rendered as meta tags. Neither is needed to be found
# eventually; both are needed to be found soon, which is the difference that
# matters for a site nothing links to yet.
#
# GOOGLE_SITE_VERIFICATION — the content="..." value from Search Console's
# HTML tag method (search.google.com/search-console). Once verified, submit
# /sitemap.xml and use URL Inspection to request indexing.
#
# NAVER_SITE_VERIFICATION — the same from Naver Search Advisor
# (searchadvisor.naver.com). Google is not where most of this page's audience
# searches, and Naver does not find a site it has not been told about.
GOOGLE_SITE_VERIFICATION=
NAVER_SITE_VERIFICATION=

# --- Notion: the team section ---------------------------------------------
# The team is read from Notion in a server component and cached for an hour,
# so these are read on the server only and never reach the browser. Without
# them the team section renders its empty state rather than failing the
# build.
#
# The integration also has to be given the database: open it in Notion,
# then ⋯ → Connections → add the integration. Notion answers 404, not 403,
# for a database it cannot see, so a correct id fails exactly like a wrong
# one until you do.
NOTION_SECRET_API_KEY=
NOTION_MEMBER_DATABASE_ID=

# --- Contact form ----------------------------------------------------------
# Web3Forms. Get a key at https://web3forms.com by entering the address the
# mail should arrive at — currently yuchan.7im@gmail.com. The key is emailed
# to that address, and it IS the destination: the API has no recipient
# field, so mail follows the key. To send somewhere else, issue a new key.
#
# This one DOES reach the browser, unlike the two above. Web3Forms refuses
# server-side callers — their API is client-side only on the free tier, and
# a call from a Vercel function comes back 403 — so the key is read here,
# handed to the form as a prop, and posted from the visitor's own browser.
# That is the model it is built for: it names the destination address, can
# read nothing back, and is in every one of their own HTML examples.
#
# Inlined at build, so changing it needs a redeploy to take effect.
#
# Free tier is 250 submissions a month. Unset, the form says it is not
# connected rather than claiming a message was received.
WEB3FORMS_ACCESS_KEY=
