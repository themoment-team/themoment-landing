# Where the contact form posts its JSON. Any service that accepts a POST
# will do — a Formspree form id, a Vercel function, an Apps Script webhook.
# Unset, the form says it is not connected rather than claiming a message
# was received. Copy this file to .env and fill it in.
VITE_CONTACT_ENDPOINT=
